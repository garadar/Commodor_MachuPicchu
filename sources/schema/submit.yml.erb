

---
batch_connect:
  template: vnc
script:
  native:
    - "-N"
    - "<%= num_tasks_per_node %>"
    - "-c"
    - "<%= nb_cpu %>"
    <%- if memtask != "default" -%>
    - "--mem"
    - "<%= memtask %>"
    <%- end -%>
